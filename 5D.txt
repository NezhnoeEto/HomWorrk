# Считываем натуральное число N
N = int(input())

# Преобразуем число N в его двоичное представление и инвертируем строку
binary_repr = bin(N)[2:][::-1]

# Выводим инвертированное двоичное представление
print(binary_repr)
